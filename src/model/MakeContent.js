function makeContent() {
    return Object.freeze({
        text : '',
        copied : false,
        withInfo : false,
        info : '',
        focus : false,
        infoFocus : false
    })
}

export default makeContent